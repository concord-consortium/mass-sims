{
  "name": "mass-sims",
  "version": "0.0.1",
  "private": true,
  "description": "Mass Sims — educational science simulations monorepo (concord-consortium/mass-sims).",
  "license": "MIT",
  "author": "Concord Consortium",
  "repository": {
    "type": "git",
    "url": "https://github.com/concord-consortium/mass-sims.git"
  },
  "engines": {
    "node": ">=24",
    "yarn": "^1.22.0"
  },
  "workspaces": [
    "packages/*",
    "simulations/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "test:scripts": "vitest run",
    "test:full": "yarn test && yarn test:scripts && yarn test:playwright:build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "lerna run typecheck",
    "typecheck:playwright": "tsc --noEmit -p playwright/tsconfig.json",
    "new-sim": "tsx scripts/new-sim.ts",
    "test:playwright": "playwright test",
    "test:playwright:build": "yarn lerna run build && playwright test",
    "test:playwright:open": "playwright test --ui",
    "playwright:install": "playwright install --with-deps chromium",
    "gen-index": "tsx scripts/gen-index.ts",
    "gen-workflows": "tsx scripts/gen-workflows.ts",
    "gen-workflows:check": "tsx scripts/gen-workflows.ts --check",
    "build:top-test": "tsx scripts/build-top-test.ts",
    "serve:top-test": "serve -p 8000 top-test",
    "clean": "lerna run clean && rimraf node_modules top-test",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@playwright/test": "~1.61.1",
    "@types/node": "^24.0.0",
    "lefthook": "^1.7.0",
    "lerna": "^4.0.0",
    "rimraf": "^5.0.0",
    "sass": "^1.80.0",
    "serve": "^14.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
